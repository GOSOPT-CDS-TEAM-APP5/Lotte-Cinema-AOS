<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/img_item_trailer"
        android:layout_width="138dp"
        android:layout_height="94dp"
        android:src="@drawable/img_trailer1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="8dp"
        />

    <TextView
        android:id="@+id/tv_item_trailer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="티저 예고편"
        android:textSize="9dp"
        android:textColor="@color/g_100"
        app:layout_constraintTop_toBottomOf="@id/img_item_trailer"
        app:layout_constraintStart_toStartOf="@id/img_item_trailer"
        app:layout_constraintEnd_toEndOf="@id/img_item_trailer"
        android:layout_marginTop="9dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>